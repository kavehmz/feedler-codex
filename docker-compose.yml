services:
  feedler:
    build: .
    ports:
      - "8080:8080"
    environment:
      DB_PATH: /app/data/feedler.db
      OPML_PATH: /app/Feeds.opml
      TZ: Europe/Berlin
    volumes:
      - feedler-data:/app/data
    restart: unless-stopped

volumes:
  feedler-data:
